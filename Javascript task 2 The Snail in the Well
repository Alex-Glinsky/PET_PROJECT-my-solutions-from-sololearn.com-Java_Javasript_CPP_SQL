function main() {
    var depth = parseInt(readLine(), 10);
    //your code goes here
    var day=0;
    var total=0;

    while (total<depth){
        total+=7;
        day++;
        if (total>=depth){
            console.log(day);
            break;

        }

            total-=2;


    }


    }
